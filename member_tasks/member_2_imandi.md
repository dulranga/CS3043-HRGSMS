# Member 2 — Imandi: rooms, availability and reservations

Planning checklist; checked rows describe prior verified work, not proof that the amended target schema is implemented. The `M2-Sxx` IDs are commit-sized planning IDs; keep published IDs stable. Member 2 owns `room_type`, `amenity`, `room_type_amenity`, `booking`, legacy `booking_status_history`, `room`, `room_block`, and target `booking_room_line`, its status/revision histories and `booking_room_assignment`. SRS Table 40 is the original ER transcription; §6.1.4 gives the multi-room target. The target has no `booking.room_id`, `room.booking_id` or `booking_room_line.room_id`; derive occupancy through open assignments and CHECKED_IN lines.

## Workflow for each subtask

1. Follow the shared `skynest-member-task-workflow` skill. Read `AGENTS.md`, `README.md`, `memory.md`, `member_work_log.md`, this file, `member_summary_table.md`, and the relevant SRS §4.2/§4.4/§6.1 requirements. Reinspect current code/migrations and Members 1, 3 and 4's latest contracts before starting each item.
2. Resolve prerequisites before dependent SQL: Member 1's UUIDv7/actor/guest/branch contract, approved enum labels (TBD-07), decimal policy (TBD-08), timestamp policy (TBD-10), and API prefix (TBD-12). Do not silently choose these. Implement approved SQL in migration files with focused verification, and explain the relevant database concepts in the handoff.
3. Keep each item to its listed slice plus focused tests/docs. Verify migrations on a clean temporary PostgreSQL database; run negative and two-session tests for overlap/transactions. Run `npm run build:backend` for API changes and `npm run build:frontend` for UI changes; both for shared changes. Add focused tests if the repo has no test command.
4. Recheck the affected SRS section and implementation; update Member 2's section in `member_work_log.md` and any durable `memory.md` decision. Mark the checkbox only with evidence and supply a copy-ready human commit message/PR description. Agents must not branch, commit, push or create a PR.

## Ordered commit-sized subtasks

| Status | ID | Deliverable and completion check |
|---|---|---|
| [x] | M2-S01 | Historical 18 September reservation handoff recorded shared types, transitions and ownership; its pointer/five-state room decisions are superseded by the 24 September target amendment in the linked contract. This checked row does not complete the new design. |
| [x] | M2-S02 | Create `room_type`, `amenity` and composite-key `room_type_amenity` per Table 40. Done when keys, FK, capacity/rate checks and duplicate-link tests pass. Depends on migration tooling and type decisions. |
| [x] | M2-S03 | Historical single-room `booking` and `booking_status_history` migration using Member 1 guest/actor keys. Its tests passed for the old contract; M2-S24/S25/S27 must backfill and retire room-specific header columns before new booking writes. |
| [x] | M2-S04 | Historical migration created `room`/`room_block` with branch/type/booking FKs and the former nullable pointer, branch-scoped room numbers and valid blocks. Tests passed for that schema; M2-S22/S23 will correct it without rewriting this completed migration. |
| [x] | M2-S05 | Historical assignment migration with booking/room FKs and at most one open assignment per booking. Tests passed for that structure; M2-S26 must rekey it per line and replace the index without rewriting this migration. |
| [ ] | M2-S24 | Add `booking_room_line` with UUIDv7 line/booking FK, line dates, guest count, `numeric(12,2)` LKR rate and five line statuses; backfill exactly one line for each valid legacy booking. Done when clean/upgrade tests preserve amounts/dates and two lines under one booking are valid. Depends on M2-S03 and Member 1 parent keys. |
| [ ] | M2-S25 | Add immutable line-status and line-value revision histories; backfill only provable legacy transition evidence and preserve original `booking_status_history`. Done when status transitions, date/rate edits and actor/reason/time evidence are tested without rewriting charged nights. Depends on M2-S24. |
| [ ] | M2-S26 | Rekey `booking_room_assignment` from booking to line, add actual occupancy instants, backfill/reconcile existing rows, replace one-open-per-booking with one-open-per-line uniqueness. Done when two active lines per booking work, a duplicate open row per line fails and ambiguous upgrades stop for repair. Depends on M2-S24/S25. |
| [ ] | M2-S22 | Remove legacy `room.booking_id` after verifying every non-null pointer against an open CHECKED_IN line assignment and no room has two checked-in occupants. Expose a normal occupancy view over line assignments. Done when upgrade/fresh-chain tests preserve history, reject mismatches and confirm no stored pointer remains. Depends on M2-S26; coordinate Members 3–5. |
| [ ] | M2-S23 | Add a corrective migration converting stored room status to physical READY/CLEANING/OUT_OF_SERVICE. For each legacy RESERVED room, verify a provable active booking assignment or an explicit dated manual block; do not invent a block for an assignment-backed reservation. Verify OCCUPIED rooms have checked-in assignments; reject missing/contradictory evidence and preserve legacy room-history labels. Done when both RESERVED evidence cases, ambiguous-row rejection, upgrade/fresh-chain tests and Member 3's physical-condition history contract pass. Depends on M2-S22 and Member 3 review. |
| [ ] | M2-S27 | Retire writable booking-header dates, guest count, rate, status and actual-stay timestamps after backfill; provide a read-only booking summary with line counts/date span/guest total and mixed progress. Done when no duplicate writable values remain, legacy histories survive and all dependent readers use lines. Depends on M2-S25/S26; coordinate Members 1, 3–5. |
| [ ] | M2-S06 | Add database-side same-room/date overlap, one-open-assignment-per-active-line, same-branch and line lifecycle guards with Members 3/4; enforce one checked-in line per physical room. Guard new/edited dated blocks against current overlapping BOOKED/CHECKED_IN assignments and reject room, branch or room-type deactivation or room OUT_OF_SERVICE while affected current assignments remain; coordinate branch updates with Member 1. Booking writes must check blocks and room/branch/type active states under the same concurrency contract. Done when direct invalid writes, AT-23/AT-25 two-session races and simultaneous overlapping reservations fail, two non-conflicting rooms under one booking and adjacent stays pass. Depends on M2-S22/S23/S27 and Member 1 branch schema. |
| [ ] | M2-S28 | Add the DBR-038 capacity/type-edit guards for existing active assignments: reject lowering `room_type.capacity` below any current BOOKED/CHECKED_IN assigned line's guest count, and reject any `room.room_type_id` change while that room has such a line. Use the M2-S06 lock order for direct SQL and concurrent booking/catalogue edits; do not alter historical lines or rate snapshots. Done when AT-27 invalid, valid and two-session cases pass. Implement before M2-S07/S08/S10. Depends on M2-S06. |
| [ ] | M2-S07 | Add chain-wide room-type/amenity catalogue API with validation; only CHAIN_MANAGER may write names, capacity, base rates, amenities or active state, while permitted staff/guests may read. Reject room-type deactivation while its rooms have current BOOKED/CHECKED_IN assignments and capacity reductions forbidden by M2-S28; keep historical records. Done when CRUD/search, changed-price snapshot persistence, AT-24/AT-25 forbidden writes and AT-27 capacity-edit cases pass. Depends on M2-S02/S06/S28 and Member 1 auth. |
| [ ] | M2-S08 | Add own-branch room and dated room-block API for BRANCH_MANAGER; route BRANCH_MANAGER/SERVICE_STAFF physical-condition changes through Member 3's audited operation. A limited outage uses a block, not a temporary OUT_OF_SERVICE value; list affected lines and require reassignment/cancellation before conflicting block, deactivation or OUT_OF_SERVICE writes. Reject any room-type reassignment while that room has a current BOOKED/CHECKED_IN line under M2-S28. Done when room-number uniqueness, block dates/overlap, invalid condition, active-assignment, booked-room type change and cross-branch requests (AT-23/AT-24/AT-27) pass. Depends on M2-S06/S23/S28, Member 3 M3-S18 and Member 1 auth. |
| [ ] | M2-S09 | Add the SRS's parameterized `fn_available_rooms` and availability API using requested branch, half-open dates, capacity, immediate-check-in flag, open line assignments, physical condition, room blocks and active room/branch/type flags; do not implement a date-dependent ordinary view. Done when capacity, inactive room/branch/type, OUT_OF_SERVICE room, block, adjacent and overlapping-date cases pass; current CLEANING blocks immediate check-in but not a non-overlapping future stay. Depends on M2-S06. |
| [ ] | M2-S10 | Add staff booking-create transaction/API accepting room lines with individual dates/guest counts; at confirmation recheck active room/branch/type, catalogue rates and Member 1's latest published chain-wide billing policy effective on the Asia/Colombo date, ordered by effective date/publication time/ID so a same-date correction wins only for new bookings. Re-quote/reconfirm if rates or policy changed. Create one header, all lines/assignments/histories and Member 4's single DRAFT invoice with the selected immutable policy FK atomically; no applicable policy or an inactive room/branch/type blocks booking. Done when Single+Double lines snapshot different type rates (AT-01), two Single lines snapshot the same type rate (AT-26), stale quote/policy, inactive parent and client-supplied rate spoofing fail, same-date correction keeps the old booking FK, any failed line rolls back all and concurrent conflicts fail. Depends on M2-S06/S09/S28, M1-S19 and Member 4's invoice-create contract. |
| [ ] | M2-S11 | Add staff booking list/detail API with branch filtering and all current/historical room lines and assignments. Done when cross-branch reads fail and a two-room booking displays both rooms without duplicate header rows. Depends on M2-S10. |
| [ ] | M2-S12 | Add staff line add/change and room-move transaction/API; pre-check-in date/rate changes are revisioned and cause Member 4's DRAFT bill to refresh, while checked-in dates/rates stay fixed and approved differences use explicit adjustment lines. Line cancellation/removal is Member 4's controlled workflow, never hard deletion. Done when old values/assignments remain queryable, changed lines recheck overlap/branch/capacity and active room/branch/type, paid-down changes can surface a credit, other lines stay intact and failures roll back. Depends on M2-S10 and Member 4's draft-bill contract. |
| [ ] | M2-S13 | Add online guest multi-room booking-create API using authenticated `guest_account`, not a submitted `guest_id`; confirm current per-line base rates and the effective billing-policy quote before snapshot/creation. Done when two-room own booking succeeds, a stale catalogue or policy quote requires reconfirmation and spoofed guest IDs/rates fail. Depends on M2-S10 and Member 1 guest auth. |
| [ ] | M2-S14 | Add online guest own-booking list/detail API. Done when guessed booking IDs and other guests' records are denied. Depends on M2-S11/S13. |
| [ ] | M2-S15 | Build room-type/amenity and room/block administration UI with shadcn primitives: chain-wide catalogue editing is CHAIN_MANAGER-only, branch room/block editing is BRANCH_MANAGER own-branch only, and conflicting maintenance or capacity/type changes show affected reservations. Done when AT-23/AT-24/AT-27 permissions/conflicts, validation, responsive layout and frontend build pass. Depends on M2-S07/S08. |
| [ ] | M2-S16 | Build availability search UI for staff and online guests with selection of multiple rooms. Done when per-line dates/capacity, running selection and conflict/empty states are tested. Depends on M2-S09. |
| [ ] | M2-S17 | Build staff booking-create UI with add/remove room lines, per-line base rates and provisional combined quote under the selected policy; surface re-quote/reconfirmation if the catalogue or policy changes before confirmation. Keep cancellation/checkout with Member 4. Done when two-room validation, quote freshness, conflict handling and frontend build pass. Depends on M2-S10. |
| [ ] | M2-S18 | Build staff booking list/detail UI with all lines and assignment history. Done when branch denial, two-room display and historical assignments pass. Depends on M2-S11. |
| [ ] | M2-S19 | Build staff room-line add/change/move UI; hand cancellation actions to Member 4. Done when unaffected lines stay visible and conflict/rollback responses are clear; frontend build passes. Depends on M2-S12. |
| [ ] | M2-S20 | Build online guest direct multi-room booking UI with per-line base-rate/combined policy quote, explicit reconfirmation after any pre-confirmation rate or policy change and no-payment-gateway messaging. Done when it uses account ownership, handles stale quotes/any-line conflicts and passes frontend build. Depends on M2-S13. |
| [ ] | M2-S21 | Build online My Bookings list/detail UI showing all room lines and their states with only own-record actions. Done when guessed IDs are denied and frontend build passes. Depends on M2-S14. |

Handoff: Member 3 consumes line-based check-in/occupancy and physical room-condition transitions; Member 4 consumes per-line rates/status events and closes only affected assignments; Member 5 reports from line/assignment history and derived occupancy. Member 1 authorizes the header guest, not a client-supplied line owner. Publish the amended contract and corrective-migration result before consumers implement new writes.

M2-S01 working handoff: [reservation contract](m2_s01_reservation_contract.md). Imandi reports that Members 1, 3 and 4 agree to follow Member 2's documented reservation ownership and delegated the shared value choices recorded there and in SRS §6.1.4/Appendix C. Their own M1-S01/M3-S01/M4-S01 checklist items retain separate acceptance checks. The ordered migration runner remains M1-S02 work; M2-S02 SQL can be verified in an isolated schema meanwhile.

## Completion notes

The dated notes below describe what was implemented and verified before the normalized room amendment. Their pointer and five-state descriptions are historical evidence; M2-S22/S23/S06 remain open and no code has been changed for the new target.

M2-S01 (18 September 2026): Imandi delegated the shared choices and reported agreement from Members 1, 3 and 4. [The reservation contract](m2_s01_reservation_contract.md), their task handoffs, SRS §6.1.4/§6.1.8/Appendix C, summary table and memory now state the selected labels/transitions, rate, UUIDv7, timestamp, actor, route and transaction ownership. Remaining owner-specific/evaluator decisions are identified rather than inferred from the ER.

M2-S02 (18 September 2026): [Catalogue migration](../backend/migrations/m2_001_room_catalogue.sql) creates Table 40's `room_type`, `amenity` and composite-key `room_type_amenity`. `npm run test:m2-catalogue --workspace backend` passed against an isolated PostgreSQL 18.6 schema with rollback; `npm run build:backend` passed. The shared ordered migration runner remains Member 1's M1-S02 integration work; this migration was not applied to the application schema.

M2-S03 (19 September 2026): [Booking migration](../backend/migrations/m2_002_booking.sql) creates the approved booking channel/status enums, `booking` and `booking_status_history`, including UUIDv7, reference/date/count/rate/time, transition and restricted guest/actor/history FK constraints. `npm run test:m2-booking --workspace backend` passed in a rolled-back PostgreSQL 18 scratch schema using minimal Member 1 parent fixtures; `npm run test:m2-catalogue --workspace backend` and `npm run build:backend` also passed. The real ordered chain still depends on Member 1's `guest`/`user_account` migrations. No booking API/write path was added; operational writes remain gated by M2-S06 safeguards.

M2-S04 (19 September 2026): [Room inventory migration](../backend/migrations/m2_003_room_inventory.sql) creates the approved room-status enum, `room` and `room_block` with UUIDv7 IDs, required branch/type/actor links, the nullable current-stay booking pointer, branch-scoped room-number uniqueness and valid block intervals. `npm run test:m2-rooms --workspace backend` passed after applying M2-S02 through M2-S04 in one rolled-back PostgreSQL 18 scratch schema; the M2-S02/M2-S03 regressions and `npm run build:backend` also passed. The production chain still needs Member 1's `branch`/`user_account` migrations. M2-S05 now supplies assignments, while M2-S06 retains overlap and pointer-state guards.

M2-S05 (20 September 2026): [Assignment migration](../backend/migrations/m2_004_booking_room_assignment.sql) creates the approved booking-room history table with UUIDv7, required booking/room FKs, ordered assignment timestamps and a partial unique index allowing at most one open row per booking. `npm run test:m2-assignments --workspace backend` passed sequential history, invalid-row and two-session simultaneous-open tests; M2-S02 through M2-S04 regressions and `npm run build:backend` also passed. M2-S06 still owns same-room stay-date overlap, active-booking lifecycle and `room.booking_id` consistency guards.

When checking a later row, add a brief evidence note here and the detailed entry under Member 2 in `member_work_log.md`.
