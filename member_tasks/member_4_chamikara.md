# Member 4 — Chamikara: billing, payments and end-of-stay

Planning checklist, not evidence of implementation. Each `M4-Sxx` is intended as one independently reviewable human commit. Member 4 owns `invoice`, `invoice_line` and `payment` (SRS Table 40), plus checkout/cancellation/no-show orchestration. The ER has no `invoice.total_amount`; derive totals from protected lines unless a separately approved amendment changes that.

## Workflow for each subtask

1. Follow the shared `skynest-member-task-workflow` skill. Read `AGENTS.md`, `README.md`, `memory.md`, `member_work_log.md`, this file, `member_summary_table.md`, and SRS §4.7–§4.8/§6.1. Reinspect live schema/code plus Member 1's actor/config/audit, Member 2's booking/assignment and Member 3's room/service contracts before each task.
2. Apply SRS §4.7.4/§6.1.4's fixed Version 1 formula, one-invoice lifecycle, linked immutable billing-policy version and refund rules. Review its working payment/line-type/exact-number proposals with affected owners under Appendix C before dependent SQL; production policy values still require authorized approval. Coordinate any booking/room/status-history write with its primary owner.
3. Implement one row's behavior and focused tests. Migrations run on a clean temporary PostgreSQL DB; test partial payments, concurrent final payment and transaction rollback. Run `npm run build:backend` and/or `npm run build:frontend` for affected packages. If test infrastructure is absent, add focused coverage rather than marking an untested item done.
4. Recheck the SRS and current implementation; record verification under Member 4 in `member_work_log.md`, update `memory.md` only for durable decisions, mark complete only with evidence, and give a copy-ready human commit message/PR description. Agents do not branch, commit, push or create PRs.

## Ordered commit-sized subtasks

| Status | ID | Deliverable and completion check |
|---|---|---|
| [ ] | M4-S01 | Publish the multi-room financial contract with Members 1–3 using SRS §4.7.4/§6.1.4: one DRAFT→FINAL invoice per booking with a fixed `billing_policy_id`, fixed per-line base rates × reserved nights, policy-based discounts/fees/no-show grace, ordered rounding, zero-balance checkout and manual PAYMENT/REFUND credits. Review the proposed payment status/method, invoice-line type and `numeric` domains rather than inventing new labels, and include same-date policy correction in the handoff. Done when Appendix C review and independent two-room totals/old-versus-corrected-policy examples are recorded. |
| [ ] | M4-S02 | Create `invoice`/`invoice_line` with Table 40 keys/FKs and reviewed SRS §6.1.4 line-type/`numeric(14,2)` contract, unique `invoice.booking_id`, non-null `billing_policy_id` FK to Member 1's immutable version, DRAFT/FINAL states and nullable room-line attribution FK; require any linked room line to belong to the invoice booking. Keep invoice number/`issued_at` absent until FINAL. Done when invalid line signs/types, missing/invalid policy FK, cross-booking lines, duplicate invoice rows/numbers and FINAL line edits fail; no `invoice.total_amount` added. Depends on M4-S01, M1-S19 and Member 2 M2-S24. |
| [ ] | M4-S03 | Create `payment` with Table 40 keys, booking/actor FKs, required PAYMENT/REFUND kind, reviewed SUCCESSFUL/FAILED/REVERSED and CASH/BANK_TRANSFER domains, positive LKR `numeric(14,2)` amount and receipt/transfer references. Done when invalid kind/amount/status/method, duplicate or missing reference and reversal/refund audit cases are tested. Depends on M4-S01 and Member 1 actor key. |
| [ ] | M4-S04 | Implement deterministic room charges from each BOOKED/CHECKED_IN/CHECKED_OUT line's fixed base-rate snapshot × reserved nights (BOOKED provisional), excluding CANCELLED/NO_SHOW room nights; sum rounded non-void service usage once. Use only the invoice-linked billing-policy version for discount cap, percentage service charge/tax and flat fees in §4.7.4 order; round each invoice line to two LKR decimals. Early departure retains reserved nights; approved post-check-in differences use explicit adjustments. Done when a mixed-type two-rate booking and a same-type equal-base-rate booking, changed pre-arrival dates, partial cancellation/no-show, early/late checkout, policy-version change and exact rounding reconcile. Depends on M4-S02, Member 2 line contract and Member 3 M3-S04's usage schema/test fixtures, not M3-S09's completed recording feature. |
| [ ] | M4-S05 | Implement audited DRAFT invoice creation/refresh and single FINAL issuance using the shared balance calculation and the invoice's fixed policy FK. Partial checkout keeps DRAFT; issue FINAL only after all lines are terminal, all charges recorded and net balance zero, assigning its unique number/`issued_at` then. Provide a booking-confirmation hook for Member 2 to create the DRAFT row with the effective policy in the same transaction. Done when missing-policy rollback, retry behavior, later draft charges, duplicate prevention and immutable FINAL lines are tested. Depends on M4-S03/S04. |
| [ ] | M4-S06 | Expose invoice/detail and PAYMENT/REFUND-history read API with branch-scoped staff access and online guests limited to their own linked bookings; label DRAFT as provisional and credits distinctly. Done when cross-branch/guest reads fail and totals match signed lines/net payments. Depends on M4-S05 and Member 1 guest ownership guard. |
| [ ] | M4-S07 | Implement locked PAYMENT/REFUND posting using M4-S05's shared balance calculation: signed invoice total − successful payments + successful refunds; failed/reversed records do not count. Allow a staff-approved manual refund only up to an existing credit, and reject a new payment above positive balance. Done when three partial payments, a charge reduction creating credit, manual refund, failed records, duplicate references and concurrent posting reconcile. Depends on M4-S03/S05. |
| [ ] | M4-S08 | Expose payment/refund API with validation, staff authorization and safe errors; no automatic gateway/refund. Done when overpay/over-refund, zero/negative and forbidden requests fail and receipts use stable references. Depends on M4-S07. |
| [ ] | M4-S09 | Implement one-line checkout transaction: lock booking/selected line/assignment/room/DRAFT invoice, require consolidated balance exactly zero, end its occupancy segment, close its assignment, set only that line CHECKED_OUT and room CLEANING through Member 3's internal condition operation, and write line/room histories. Authorized own-branch FRONT_DESK checkout may invoke this internal CLEANING transition; it does not grant a general room-condition edit right. Show a provisional statement/payment receipt while another line remains active; finalize the one invoice if this makes every line terminal and settled. Done when another line remains active, a positive balance or negative credit blocks checkout and injected failure rolls all changes back. Depends on M4-S07, Member 2 M2-S06 and Member 3 M3-S18. |
| [ ] | M4-S10 | Expose line-specific checkout API with branch/role guards and explicit repeated-request behavior. Done when partial two-room checkout, positive-balance and repeat tests pass. Depends on M4-S09. |
| [ ] | M4-S11 | Implement per-line and whole-booking cancellation before each BOOKED line's linked-policy no-show cutoff: remove affected provisional room-night charges, add only that policy's flat cancellation fee, refresh DRAFT, close affected assignments and release inventory without deleting history. Whole-booking cancellation requires every line to be eligible. Done when one room remains booked, before/at-cutoff boundaries, later-policy fee stability, prior-payment credit, whole-booking rollback and guest ownership tests pass. Depends on Members 1/2 and M4-S01/S04. |
| [ ] | M4-S12 | Implement per-line no-show transition with cutoff at local 00:00 on current `stay_start_date + invoice-linked no_show_grace_days`, history and affected-assignment closure; replace provisional room nights with only that policy version's flat no-show fee on DRAFT. Done when one line remains valid, one-day demo boundary/date revision/later-policy changes, credit/refund path, early no-show and repeat transitions are tested. Depends on Member 2 line/assignment contract and M4-S04. |
| [ ] | M4-S13 | Build invoice-detail UI using shadcn primitives, showing separately priced room lines, adjustment order, DRAFT/provisional versus FINAL/issued state, booking-wide charges and exact consolidated totals. Done when two-room/partial-checkout and role-scope checks and frontend build pass. Depends on M4-S06. |
| [ ] | M4-S14 | Build payment UI with partial-payment entry, signed balance/credit and staff-only manual refund/failure states. Done when exact displayed totals and frontend build pass. Depends on M4-S08. |
| [ ] | M4-S15 | Build staff per-line checkout UI with consolidated exact-zero balance guard, DRAFT provisional statement and confirmation; show FINAL only after last terminal line and settlement. Done when remaining-room, positive-balance, credit and rollback states display safely. Depends on M4-S10. |
| [ ] | M4-S16 | Build staff per-line or whole-booking cancellation UI with policy eligibility, fee display and confirmation. Done when unaffected lines and denied/cancelled states pass. Depends on M4-S11. |
| [ ] | M4-S17 | Build staff per-line no-show UI with cutoff feedback and confirmation. Done when another line survives and early/repeated transition states pass. Depends on M4-S12. |
| [ ] | M4-S18 | Add online own-booking per-line/whole cancellation controls to Member 2's My Bookings UI. Done when cross-account denial, policy messages and frontend build pass. Depends on M4-S11 and Member 2 own-booking UI. |

Handoff: publish invoice/payment/charge and cancellation status contracts for Member 5's reports and Member 1's own-payment history. Consume Member 1's typed `billing_policy` publication/read contract and audit interface; do not add duplicate policy, `system_config` or `audit_log` tables or parse generic text settings for financial rules.

### Member 2 reservation handoff (M2-S01)

Use [Imandi's amended reservation contract](m2_s01_reservation_contract.md): Member 4 orchestrates per-line checkout/cancellation/no-show and optional whole-booking cancellation, closes only affected assignments without deleting history, and writes line-status events in the same transaction. Checkout sets that room CLEANING through Member 3's condition/history operation; derived occupancy clears only for that line. Cancellation/no-show of a future BOOKED line changes no physical room condition. One DRAFT→FINAL invoice per booking follows SRS §4.7.4; room charges derive from each line's fixed base rate and reserved nights, and service usage is counted once. A credit requires a manual staff REFUND before checkout/finalization. Member 1 supplies actor/audit and guest-ownership guards. The actor target and LKR `numeric(12,2)` rate scale remain in M2-S01. SRS §6.1.4 now proposes payment/line-type/amount domains; Member 4 must confirm them with owners, plus production policy values and tested lock contracts. This handoff does not check off M4-S01.

## Completion notes

When checking a row, add a brief evidence note here and the detailed entry under Member 4 in `member_work_log.md`. No subtasks are marked complete by this plan.
